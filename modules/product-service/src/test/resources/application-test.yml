spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product-service-test
    username: eventshop
    password: eventshop
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
