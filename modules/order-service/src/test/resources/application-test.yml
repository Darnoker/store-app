spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eventshop-test
    username: eventshop
    password: eventshop
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
